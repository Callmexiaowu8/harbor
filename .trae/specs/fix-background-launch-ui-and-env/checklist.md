- [x] 终端启动和后台启动按钮为纯图标样式，无文字标签
- [x] 终端启动按钮使用 `apple.terminal` 图标，后台启动按钮使用 `play.fill` 图标
- [x] 两个启动按钮视觉有明显区分（描边 vs 填充风格）
- [x] 按钮尺寸与编辑按钮一致（30x30）
- [x] 后台启动使用 `/bin/zsh -l -i -c` 执行命令
- [x] 启动命令 `PORT=3001 npm run dev` 在后台启动中能正确找到 npm
- [x] 构建通过
